import React from 'react'

function Logout() {
  return (
    <div className="container">
        <label> Logout from app </label>
        <button> Logout </button>
    </div>
  )
}

export default Logout